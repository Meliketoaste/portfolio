---
import { Code } from 'astro:components'
import { Badge } from '@/components/ui/badge'
import {
  Card,
  CardContent,
  CardDescription,
  CardFooter,
  CardHeader,
  CardTitle,
} from '@/components/ui/card'

import { Image } from 'astro:assets'
---

<Card className=" w-full p-4 flex flex-row">
  <div class="w-1/2 flex flex-col">
    <CardHeader>
      <CardTitle
        className="bg-gradient-to-b from-foreground via-foreground/90 to-foreground/70 to-90% bg-clip-text font-calsans text-4xl font-bold text-transparent sm:text-4xl"
        >Luadec</CardTitle
      >
      <CardDescription className="text-base text-foreground"
        >Declarative configuration system with lua</CardDescription
      >
    </CardHeader>
    <CardContent className="text-muted-foreground">
      <p>
        Luadec gives you powers to configure Linux from lua files. The managers
      </p>
    </CardContent>
    <CardFooter className="flex flex-col flex-grow items-start">
      <p class="mb-2">More about it HERE</p>

      <div class="mt-auto flex gap-2">
        <Badge className="" variant="outline" size="lg"> Rust </Badge>
        <Badge className="" variant="outline" size="lg"> Lua </Badge>
        <Badge className="" variant="outline" size="lg"> woah project </Badge>
      </div>
    </CardFooter>
  </div>
  <div class="w-1/2">
    <CardContent className="p-0 py-6">
      <Code
        class="border-border border rounded-lg p-2"
        code={`local luadec = require 'luadec'

luadec.add_manager({
  name = "Debian",
  add = "sudo apt install #:?",
})

luadec.packages("Debian", {
  "neovim",
  "firefox",
})
-- and much more
`}
        lang="lua"
        theme="material-theme-ocean"
      />
    </CardContent>
  </div>
</Card>
